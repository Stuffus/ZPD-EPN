<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <style>
      body {
        overflow: hidden;
        padding-top: 80px;
        min-height: calc(100vh - 90px);
        font-family: "Montserrat", sans-serif;
        font-size: 22px;
        background: linear-gradient(to bottom, #00d2ff, #d985caa9);
        color: #fff;
      }
      .form-wrapper {
        text-align: center;
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        background-color: rgba(91, 91, 91, 0.5);
        padding:30px;
        padding-bottom: 10px;
        border-radius: 50px;
      }

      label {
        display: block;
        margin-bottom: 0px;
      }

      input[type="text"],
      input[type="password"] {
        padding: 8px;
        padding-bottom: 10px;
        font-size: 20px;
        border-radius: 50px;
        border: 5px solid #ccc;
        margin-top: 15px;
        margin-bottom: 15px;
        display: block;
        margin: 10 auto;
        max-width: 100%;
        min-height: 30px;
        border: 1px solid #000;
        background-color: rgba(0, 0, 0, 0.4);
        color: #fff;
        outline: thick solid;
      }

      input[type="submit"] {
        background-color: #007bff;
        color: #fff;
        border: none;
        cursor: pointer;
        padding: 8px;
        font-size: 20px;
        border-radius: 10px;
        margin-bottom: 15px;
        text-transform: capitalize;
      }

      .form-button {
        margin-top: 20px;
        text-align: center;
      }

      #login-form {
        display: inline-block;
        text-align: center;
      }

      .error-message {
        color: red;
        font-size: small;
        font-weight: bold;
        margin-top: 10px;
      }

      .password-toggle-wrapper {
        display: grid;
        justify-content: right;
        align-items: end;
      }

      .password-toggle {
        margin-left: 10px;
        display: block;
      }
    </style>
  </head>
  <body>
    <div class="form-wrapper">
      <form id="login-form" method="post" action="/login">
        <label>Ē-Pasts:
          <input type="text" name="username" required>
        </label>
        <label>Parole:
          <div class="password-toggle-wrapper">
            <input type="password" name="password" id="password-input" required>
            <label >Parādīt paroli</label>
            <input type="checkbox" class="password-toggle" id="password-toggle">
          </div>
        </label>
        <div class="form-button">
          <input type="submit" value="ielogoties">
        </div>
      </form>
    </div>
  </body>
</html>

<script>
  const passwordInput = document.getElementById('password-input');
  const passwordToggle = document.getElementById('password-toggle');

  passwordToggle.addEventListener('change', function() {
    if (passwordToggle.checked) {
      passwordInput.type = 'text';
    } else {
      passwordInput.type = 'password';
    }
  });
</script>